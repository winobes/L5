if (current_level == 0) {
	if (player.y > 250 + player.height && player.y < 350  && player.x > 750 && player.x < 790 - player.width) {
		player.dy = -player.dy;
		if (player.y > 260) {
			current_level = 1;
			change_level = true;
		}
	}

}
