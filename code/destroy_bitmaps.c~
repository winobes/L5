

for (i = 0; i < nrooms; i++) {
	if (room[i].initialized) {

		al_destroy_bitmap(room[i].background);
		al_destroy_bitmap(room[i].background_buffer);

		for (j = 0; j < room[i].nwalls; j++) {
			al_destroy_bitmap(room[i].wall[j].sprite);

		}
	}
}
